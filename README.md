# Task-Manager
A task manager is a simple application built using spring boot that manager the tasks of a user. This is a RESTful application that uses Spring Framework at the backend
This application includes feature such as task creation, task reading, task updation/modification, task deletion
